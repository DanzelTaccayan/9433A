Request Header Fields
Accept-content negotiation
Accept Charset-content negotiation, resource in ascii, unicode, etc.
Accept Encoding-content negotiation, forms for giving requests
Accept Language-content negotiation, language specification
Authorization-protect from unauthorized access
Expect-2face operation (validate first)
HOST- for multiple HTML files with the same name in a single server
From- example: EMAIL
if match-check if server has same copy with code in cache
if none-match-create/ download if it has not been created yet 
if modified-since-like if match
range-specific number of bytes only
max-forwards-proxy trace (limit)
referer-where the request is originated from
